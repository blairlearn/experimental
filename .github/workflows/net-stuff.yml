name: Do stuff with .NET

on:
  workflow_dispatch:
  push:

jobs:

  do-stuff:

    runs-on: ubuntu-latest

    steps:

      - name: Hello!
        run: echo "Hello there!"

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Run coverlet
        run: |
          coverlet --version

